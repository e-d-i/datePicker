{
  "name": "datePicker",
  "version": "1.0.0",
  "description": "A simple date picker using HTML, CSS/Animate.css, JavaScript & Parcel",
  "main": "datePicker.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url /datePicker/",
    "deploy": "rm -rf dist/ && npm run build && gh-pages -d dist"
  },
  "homepage": "https://e-d-i.github.io/datePicker/",
  "keywords": [],
  "author": "e-d-i",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.23.0",
    "gh-pages": "^3.2.3"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}